import { StyleSheet } from "@react-pdf/renderer"

const styles = StyleSheet.create({
    green: {
        color: "#556B2F",
        fontSize: 11
    },
    container: {
        padding: 5,
        width: "40vw"
    }
    
})

export default styles;